[Unit]
Description=${APPNAME}
After=network.target

[Service]
Type=simple
User=${USERNAME}
Group=${USERNAME}
WorkingDirectory=${HOMEDIR}/staging
ExecStart=${REMOTEDIR}/neems-api
EnvironmentFile=${HOMEDIR}/staging/.env

[Install]
WantedBy=multi-user.target
